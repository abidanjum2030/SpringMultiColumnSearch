
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demodb?serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    generate-ddl: true
  h2:
    console:
      enabled: true
server:
  port: 8081
